[package]
name = "vm-cli"
version.workspace = true
edition.workspace = true
description = "Command-line interface for the vm version manager."

[[bin]]
name = "vm"
path = "src/main.rs"

[dependencies]
vm-core = { path = "../vm-core" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
